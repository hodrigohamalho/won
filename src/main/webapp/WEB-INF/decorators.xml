<?xml version="1.0" encoding="UTF-8"?>

<decorators defaultdir="/jsp/decorators">

    <!-- unfortunately I can't do that in a generic way, so to use
    a undecorated page, create a dir and put the pattern bellow
    -->
    <decorator name="none">
        <pattern>/jsp/dc/partials/*</pattern>
    </decorator>


    <decorator name="main" page="main.jsp">
        <pattern>/*</pattern>
    </decorator>
</decorators>
